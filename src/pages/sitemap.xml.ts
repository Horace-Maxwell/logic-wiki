import {allUnits} from '../data/catalog.mjs';
import {categories} from '../data/model.mjs';
export function GET(){const origin=(process.env.SITE_URL||'http://127.0.0.1:4321').replace(/\/$/,'');const paths=['','knowledge/','fallacies/','cases/','learn/','sources/','compare/','glossary/','editorial/',...Object.keys(categories).map(x=>`fallacies/${x}/`),...allUnits.map(x=>`wiki/${x.id}/`)];const escape=(s:string)=>s.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('"','&quot;');const urls=['zh','en'].flatMap(lang=>paths.map(p=>`<url><loc>${escape(`${origin}/${lang}/${p}`)}</loc><xhtml:link rel="alternate" hreflang="zh-CN" href="${escape(`${origin}/zh/${p}`)}"/><xhtml:link rel="alternate" hreflang="en" href="${escape(`${origin}/en/${p}`)}"/></url>`)).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">${urls}</urlset>`,{headers:{'Content-Type':'application/xml'}});}
